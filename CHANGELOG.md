# Changelog

All notable repository-level changes are documented here. Each independently
versioned distribution may also maintain package-specific release notes.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and released packages use semantic versioning.

## [Unreleased]

### Changed

- Organized the Vanilla JavaScript, npm, and Python implementations under
  `packages/` on the main branch.
- Added package-scoped CI, repository governance, and monorepo documentation.

## Package Histories

- Python/PyPI releases: [`packages/python/CHANGELOG.md`](packages/python/CHANGELOG.md)
- npm release metadata: [`packages/npm/package.json`](packages/npm/package.json)

Earlier Vanilla JavaScript and npm history remains available in Git and in the
original `js-editor` and `npm-olloweditor` branch histories.
