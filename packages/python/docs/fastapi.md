# FastAPI

Install:

```bash
pip install "olloweditor[fastapi]"
```

Use `mount_olloweditor(app)` to serve packaged assets and register `olloweditor_assets` in `Jinja2Templates.env.globals` for templates.
